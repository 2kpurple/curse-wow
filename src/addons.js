class Addons {
  constructor(name, url, icon, updateTime, desc, category) {
    this.name = name
    this.url = url
    this.icon = icon
    this.updateTime = updateTime
    this.desc = desc
    this.category = category
  }
}

module.exports = Addons
